{
  "name": "try-jest",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "esbuild": "^0.12.28",
    "esbuild-jest": "^0.5.0",
    "jest": "^27.2.0",
    "ts-jest": "^27.0.5",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "socket.io": "^4.2.0",
    "socket.io-client": "^4.2.0"
  }
}
